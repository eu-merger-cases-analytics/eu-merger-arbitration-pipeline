POSTGRES_USER=user
POSTGRES_PASSWORD=user
POSTGRES_DB=eu-merger-arbitration

# Superset (local testing only — change in production)
SUPERSET_SECRET_KEY=dev-only-change-me
SUPERSET_ADMIN_USER=admin
SUPERSET_ADMIN_PASSWORD=admin
SUPERSET_ADMIN_EMAIL=admin@local

# Airflow (local testing only — change in production)
# Generate new key: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW_FERNET_KEY=46BKJo0lfsKqgqE0hn3VI5qBKJo0lfsKqgqE0hn3VI5=
AIRFLOW_ADMIN_USER=admin
AIRFLOW_ADMIN_PASSWORD=admin
